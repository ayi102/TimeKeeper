# Copy to deploy/mail.env and fill in. Keep mail.env private (chmod 600);
# it is git-ignored and not synced from the dev machine.
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=you@example.com
MAIL_PASSWORD=your-app-password
MAIL_TO=aliI@facts-eng.com
MAIL_FROM=you@example.com
